type: object
properties:
  code:
    type: integer
    example: 500
    description: HTTP status code
  message:
    type: string
    example: Internal server error
    description: A message indicating the error
required:
  - code
  - message
